apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: ai-loadbalancer
  labels:
    app: inventory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8084"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
      - name: inventory-service
        image: inventory-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8084
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "docker"
        - name: SERVICE_HOST
          value: "inventory-service"
        - name: DB_HOST
          value: "postgres.ai-loadbalancer.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "inventory_db"
        - name: DB_USERNAME
          value: "ecommerce_user"
        - name: DB_PASSWORD
          value: "ecommerce_pass"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres.ai-loadbalancer.svc.cluster.local:5432/inventory_db"
        - name: SPRING_DATASOURCE_USERNAME
          value: "ecommerce_user"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "ecommerce_pass"
        - name: REDIS_HOST
          value: "redis.ai-loadbalancer.svc.cluster.local"
        - name: REDIS_PORT
          value: "6379"
        - name: KAFKA_BROKERS
          value: "kafka.ai-loadbalancer.svc.cluster.local:9092"
        - name: JWT_SECRET
          value: "mySecretKey123456789012345678901234567890123456789012345678901234567890"
        - name: JWT_EXPIRATION
          value: "86400000"
        - name: SERVER_PORT
          value: "8084"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: ai-loadbalancer
  labels:
    app: inventory-service
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
  selector:
    app: inventory-service
